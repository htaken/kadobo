{
  "name": "kadobo",
  "private": true,
  "version": "0.0.0",
  "description": "稼働記録・経費管理システム（MVP）— Cloudflare Worker + Google Apps Script",
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "shared",
    "worker",
    "gas"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
