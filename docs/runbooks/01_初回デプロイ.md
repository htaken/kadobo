# Runbook 01: 初回デプロイ

まっさらな状態から本番稼働まで。**上から順に**。各ステップ末尾の「✅ 確認」を満たしてから次へ。
値を控える欄（`____`）は各自の環境で埋める。所要目安 60〜90 分。

> 記法: `!` で始まる行は Claude Code のプロンプトにそのまま打つと会話内で実行できる。
> 素の `$` はターミナル前提のコマンド。

---

## 事前: 控える値の一覧（このセクションを埋めながら進む）

| 記号 | 値 | 取得ステップ |
|---|---|---|
| `SIGNING_SECRET` | ____ | ②-3 |
| `BOT_TOKEN`（`xoxb-…`） | ____ | ②-4 |
| `CHANNEL_ID`（`C…`） | ____ | ②-5 |
| `USER_ID`（`U…`） | ____ | ②-5 |
| `SHARED_SECRET` | ____ | ③-4 で生成 |
| `DATABASE_ID` | ____ | ③-2 |
| `WORKER_URL`（`https://kadobo-worker.<sub>.workers.dev`） | ____ | ③-6 |
| `SPREADSHEET_ID` | ____ | ⑤-1 |
| `SCRIPT_ID` | ____ | ⑤-3 |
| `EXEC_URL`（`https://script.google.com/macros/s/…/exec`） | ____ | ⑤-6 |

---

## ① ローカル検証

```
! npm install && npm test && npm run typecheck && npm run build
```

✅ 確認: 271 tests 全て pass、typecheck エラー無し、`worker` の dry-run 成功と `gas/dist/Code.js` 生成。

---

## ② Slack アプリ作成

1. https://api.slack.com/apps → **Create New App → From an app manifest** → 本人ワークスペースを選択
2. 下記 manifest を貼る（`request_url` は後で ④ で差し替えるので今はダミーのままでよい）:

   ```yaml
   display_information:
     name: 稼働記録
   features:
     bot_user:
       # bot_user.display_name は @ユーザー名に変換されるため英数字・一部記号のみ（日本語不可）。
       # 表示名を日本語にしたい場合は作成後に App Home の Display Name で変更する。
       display_name: kadobo
       always_online: true
     slash_commands:
       - command: /kado
         url: https://example.invalid/slack/commands
         description: 稼働カードの投稿・再描画・累計表示
         should_escape: false
       - command: /keihi
         url: https://example.invalid/slack/commands
         description: 経費入力（自動化フェーズで提供予定）
         should_escape: false
   oauth_config:
     scopes:
       bot:
         - commands
         - chat:write
         - files:read
         - im:write
   settings:
     interactivity:
       is_enabled: true
       request_url: https://example.invalid/slack/interactivity
     org_deploy_enabled: false
     socket_mode_enabled: false
     token_rotation_enabled: false
   ```

3. **Basic Information → App Credentials → Signing Secret** を控える → `SIGNING_SECRET`
4. **OAuth & Permissions → Install to Workspace** で承認 → **Bot User OAuth Token（`xoxb-…`）** を控える → `BOT_TOKEN`
5. Slack で私用チャンネル **`#稼働記録`**（private）を作り、`/invite @稼働記録` で Bot を招待。
   - `CHANNEL_ID`: チャンネル名クリック → 一番下の Channel ID（`C…`）
   - `USER_ID`: 自分のプロフィール → その他 → メンバー ID をコピー（`U…`）

✅ 確認: `SIGNING_SECRET` / `BOT_TOKEN` / `CHANNEL_ID` / `USER_ID` の 4 つが埋まった。

---

## ③ Cloudflare（Worker）デプロイ

1. ログイン（ブラウザ認証）:
   ```
   ! cd /Users/takenouchiharuhi/projects/kadobo/worker && npx wrangler login
   ```
2. D1 データベース作成:
   ```
   ! cd /Users/takenouchiharuhi/projects/kadobo/worker && npx wrangler d1 create kadobo-journal
   ```
   出力の `database_id` を控える → `DATABASE_ID`
3. `worker/wrangler.jsonc` の `"database_id": "REPLACE_WITH_WRANGLER_D1_CREATE_OUTPUT"` を `DATABASE_ID` に置き換える（このファイル編集は伴走者に依頼可）。
4. マイグレーション適用（本番 D1）と共有シークレット生成:
   ```
   ! cd /Users/takenouchiharuhi/projects/kadobo/worker && npx wrangler d1 migrations apply DB --remote
   ! openssl rand -hex 32          # 出力を SHARED_SECRET として控える（GAS 側と同じ値を使う）
   ```
   次にシークレットを投入する。**`wrangler secret put` は値の入力プロンプトが出るため、
   Claude Code の `!`（非対話）では入力できない。通常のターミナル（Terminal.app 等）で実行すること**
   （`echo "値" | ...` は履歴・会話にトークンが残るので避ける）:
   ```
   cd /Users/takenouchiharuhi/projects/kadobo/worker
   npx wrangler secret put SLACK_SIGNING_SECRET   # ② の SIGNING_SECRET を入力
   npx wrangler secret put SLACK_BOT_TOKEN         # ② の BOT_TOKEN
   npx wrangler secret put GAS_SHARED_SECRET        # ③-4 の SHARED_SECRET
   npx wrangler secret put GAS_URL                  # まだ不明。ダミー https://script.google.com/pending を入れる
   ```
5. デプロイ:
   ```
   ! cd /Users/takenouchiharuhi/projects/kadobo/worker && npx wrangler deploy
   ```
6. 出力の `https://kadobo-worker.<subdomain>.workers.dev` を控える → `WORKER_URL`

✅ 確認: `GET WORKER_URL/` が 404（正常。POST 専用のため）。
```
! curl -s -o /dev/null -w "%{http_code}\n" https://kadobo-worker.<subdomain>.workers.dev/
```

---

## ④ Slack Request URL を Worker に向ける

Slack アプリ設定で 3 箇所を `WORKER_URL` ベースに更新:
- **Slash Commands** → `/kado`・`/keihi` の Request URL = `WORKER_URL/slack/commands`
- **Interactivity & Shortcuts** → Request URL = `WORKER_URL/slack/interactivity`

✅ 確認: Slash Command の URL 保存時、Slack が疎通確認する。エラーが出ないこと（署名検証が働くので Slack からの正規リクエストのみ通る）。

---

## ⑤ GAS デプロイ

1. https://drive.google.com で新規 Google スプレッドシートを作成。URL の `/d/<ここ>/edit` を控える → `SPREADSHEET_ID`
2. clasp ログイン（ブラウザ認証）:
   ```
   ! cd /Users/takenouchiharuhi/projects/kadobo/gas && npx clasp login
   ```
3. スクリプトプロジェクト作成（standalone）:
   ```
   ! cd /Users/takenouchiharuhi/projects/kadobo/gas && npx clasp create-script --title "kadobo" --type standalone --rootDir dist
   ```
   生成された `.clasp.json` の `scriptId` を控える → `SCRIPT_ID`。
   - `rootDir` が `dist` になっていること（`.clasp.json.example` と同じ）。異なる場合は `dist` に直す。
4. ビルド＆プッシュ:
   ```
   ! cd /Users/takenouchiharuhi/projects/kadobo && npm run build
   ! cd /Users/takenouchiharuhi/projects/kadobo/gas && npx clasp push
   ```
5. Script Properties を設定（GAS エディタ `npx clasp open-script` → プロジェクトの設定 ⚙ → スクリプト プロパティ）:

   | プロパティ名 | 値 |
   |---|---|
   | `SPREADSHEET_ID` | ⑤-1 の値 |
   | `SLACK_BOT_TOKEN` | ② の `BOT_TOKEN` |
   | `GAS_SHARED_SECRET` | ③-4 の `SHARED_SECRET`（**Worker と同一**） |
   | `WORKER_STATUS_URL` | `WORKER_URL/internal/status` |
   | `SLACK_CHANNEL_ID` | ② の `CHANNEL_ID` |
   | `SLACK_USER_ID` | ② の `USER_ID` |
   | `CLIENT_DEFAULT` | `A社`（請求先の表示名） |

6. ウェブアプリとして公開: GAS エディタ **デプロイ → 新しいデプロイ → 種類=ウェブアプリ**、
   **実行するユーザー=自分**、**アクセスできるユーザー=全員**。発行された `/exec` URL を控える → `EXEC_URL`。
   - 初回は OAuth 同意が出る（`appsscript.json` の 5 スコープ）。承認する。
7. 初期化関数を 1 回ずつ実行（エディタ上部の関数選択 → 実行）:
   - `setupSpreadsheet` … 6 シート（生ログ/日次集計/単価マスタ/月次請求/経費台帳/内部）を作成
   - `installTriggers` … 朝(07)・夜(22)・月次(1 日 06) のトリガーを設定
8. **HMAC 相互運用の実地確認**（受入試験 §7）: `../受入試験チェックリスト.md` の手順で、
   `Utilities.computeHmacSha256Signature` が `shared/test/vectors/envelope.json` の sig を再現することを 1 回確認。

✅ 確認: スプレッドシートに 6 シートができ、トリガーが 3 つ登録され、HMAC チェックが OK。

---

## ⑥ Worker の GAS_URL を本物に上書き

```
cd /Users/takenouchiharuhi/projects/kadobo/worker && npx wrangler secret put GAS_URL   # EXEC_URL を貼る
```
Secrets の更新は即時反映（再デプロイ不要）。

✅ 確認: 単価マスタに 1 行入れておく（`client=A社`, `unit_price=<時給>`, `tax_category=不課税`,
`tax_inclusive=false`, `tax_display=なし`, `rounding=切捨`, `withholding=なし`, `valid_from=2026-09-01`, `valid_to=` 空）。
※ 税区分・源泉・消費税相当額は `../未決事項・デプロイ前確認.md` を確認のうえ確定する。

---

## ⑦ スモークテスト

Slack の `#稼働記録` で:
1. `/kado` → 当日カードが投稿される（`[開始]` ボタン）
2. `[開始]` → 数秒で `✅ 開始 HH:mm 記録済み`、生ログに `START` 1 行
3. `[休憩]`→`[再開]`→`[終了]` → カードが状態遷移、日次集計シートに当日行
4. `/kado status` → 今週・今月の累計が自分だけに見える（ephemeral）

✅ 確認: 上記が全て動く。動かない場合は [03_トラブルシューティング](03_トラブルシューティング.md) へ。

---

## ⑧ 受入試験

[../受入試験チェックリスト.md](../受入試験チェックリスト.md) を上から実施し、完了記録表を埋める。
特に「GAS 停止 → 復旧で自動再送」「休憩中終了」「修正モーダル」「不正署名/古い ts/偽封筒の拒否」を必ず通す。

---

## 本番開始

受入試験が全て OK になったら本番運用開始（要件 §8: 9 月第 1 週）。
開業日 9/1 から本番開始までの稼働は手動でシートに記録する。
